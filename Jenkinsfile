pipeline{
    agent any
    stages{
        stage('Selecte an Account') { 
            steps { 
                echo "This is a second PR"
                echo "this is a test"
            } 
        }
        stage('A second stage') { 
            steps { 
                echo "this is a test"
                echo "Adding a second output"
            } 
        }
    }
}
