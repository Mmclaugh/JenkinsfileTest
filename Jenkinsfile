pipeline{
    agent any
    stages{
        stage('Selecte an Account') { 
            steps { 
                echo "this is a test"
            } 
        }
        stage('A second stage') { 
            steps { 
                echo "this is a test"
            } 
        }
    }
}
