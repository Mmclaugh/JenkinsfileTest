pipeline{
    agent any
    stages{
        stage('Selecte an Account') { 
            steps { 
                echo "this is a test"
                echo "this is a change in a PR"
            } 
        }
    }
}
